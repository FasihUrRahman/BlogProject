@model IEnumerable<Blog.Models.UserRole>

    @{
        ViewData["Title"] = "User Roles";
    }
    <h1>New Roles</h1>
    @* <p><a asp-controller="" asp-action=""></a></p> *@
    @* To Get Values Form DB *@
    @* <table>
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model=>model.Id)</th>
                <th>@Html.DisplayNameFor(model=>model.Name)</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelitem=> item.Id)</td>
                <td>@Html.DisplayFor(modelitem=> item.Name)</td>
                <td><a asp-action="" asp-controller="">Update</a> | <a asp-action="" asp-controller="">Delete</a></td>
            </tr>
        }
            
        </tbody>
    </table> *@
<div class="container">
    <div class="row justify-content-center">
        <div class="col-6">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Name</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelitem=> item.Id)</td>
                            <td>@Html.DisplayFor(modelitem=> item.Name)</td>
                            <td><a asp-action="" asp-controller="">Update</a> | <a asp-action="" asp-controller="">Delete</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        
    </div>
</div>